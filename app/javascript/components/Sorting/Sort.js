import React from "react";

const Sort = (props) => {
    const { tasks } = props;

    const handleSort = () => {
        
    }

    return <div>
        <button onClick={handleSort}>Sort</button>
    </div>
}

export default Sort;